# Migrates: demo-chat.test.ts :: "the chat composer is available"
# The composer is a textarea with placeholder "Ask the agent or type / for
# skills..." and a Send button.
name: Demo - the chat composer is available
app: web
url: ${DM_BASE_URL}/chat                  # shipped in PR #23
session:                                   # shipped in PR #23
  cookies:
    - name: automators.session
      value: ${DM_SESSION_COOKIE}
  local_storage:
    projectId: ${DM_PROJECT_ID}
    teamId: ${DM_TEAM_ID}
steps:
  - Wait until page shows Send within 30s
  - assert: the "Ask the agent" is visible      # shipped in PRs #21/#22
